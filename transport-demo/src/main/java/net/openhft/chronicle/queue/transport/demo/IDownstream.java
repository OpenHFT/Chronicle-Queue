package net.openhft.chronicle.queue.transport.demo;

/**
 * Interface for message downstream from your core system.
 */
public interface IDownstream extends IUpstream {
    // keep downstream and upstream the same for simplicity.
}
